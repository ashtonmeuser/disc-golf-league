<% if(user.isAdmin) { %>
  <section class="place margin-top">
    <form id="place-form" action="/place" method="post">
      <div class="row">
        <input id="place-date" type="text" name="date" placeholder="Leg Close (YYYY-MM-DD)"/>
      </div>
      <div class="row">
        <input disabled class="button margin-top" id="place-submit" type="submit" value="End Leg"/>
      </div>
    </form>
  </section>
<% } %>
